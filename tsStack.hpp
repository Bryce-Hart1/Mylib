#include "tsVector.hpp"

template <typename T>
using vec = threadsafe::vec<T>;


namespace threadsafe{

    template <typename T>
    class queue{
        friend class vec<T>;
        private:

    };











}